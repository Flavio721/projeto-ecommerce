import express from 'express';

const router = express.Router();


router.post("/create"
    // Funções e middlewares para criar cupom
)
router.post("/validate"
    // Funções e middlewares para validar cupom
)

export default router;
