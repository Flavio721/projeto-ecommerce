import express from "express";


const router = express.Router();

router.post("/register", 
    // Função e middlewares de cadastro
)
router.post("/login"
    // Função e middlewares de login
)
router.post("/refresh"
    // Função e middlewares de refresh
)
router.post("/logout"
    // Função e middlewares de logout
)

export default router;